import React from 'react';

export interface BaseEditorProps {

}

export const BaseEditor : React.FC<BaseEditorProps> = (props) => {
    return (
        <div>
            base editor test live reload
        </div>
    );
}